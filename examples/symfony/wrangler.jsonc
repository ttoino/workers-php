/**
 * Wrangler config for the Symfony example (this directory).
 *
 * Use with: `npm run dev:symfony` or `npm run deploy:symfony`.
 *
 * The database is plain SQLite SQL via the $env->DB binding. Before first
 * deploy, run:
 *
 *     wrangler d1 create workers-php-symfony-db
 *     wrangler d1 execute workers-php-symfony-db --config examples/symfony/wrangler.jsonc --remote --file=examples/symfony/database/schema.sql
 *
 * Then paste the resulting database_id into the placeholder below.
 */
{
	"$schema": "../../node_modules/wrangler/config-schema.json",
	"name": "workers-php-symfony",
	"main": "worker.ts",
	"compatibility_date": "2026-05-26",
	"observability": {
		"enabled": true,
		"logs": { "enabled": true },
		"traces": { "enabled": true }
	},
	"upload_source_maps": true,
	"compatibility_flags": [
		"nodejs_compat"
	],
	"assets": {
		"directory": "./dist",
		"binding": "ASSETS",
		"run_worker_first": true
	},
	"vars": {
		"APP_ENV": "prod"
	},
	"d1_databases": [
		{
			"binding": "DB",
			"database_name": "workers-php-symfony-db",
			"database_id": "a3716b8e-775c-422c-8af8-4984afa8fab4"
		}
	],
	"rules": [
		{ "type": "Text", "globs": ["**/*.php"], "fallthrough": true }
	]
}
